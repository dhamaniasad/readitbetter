============
Installation
============

At the command line::

    $ easy_install readitbetter

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv readitbetter
    $ pip install readitbetter
